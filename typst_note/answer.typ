#text(fill: rgb("#eb3434"))[ *改编题1：*]求幂级数 $sum_(n=1)^(+oo) (-1)^n 3^n/n (x - 2)^n$ 的收敛半径与收敛域。


#text(fill: rgb("#eb3434"))[ *解答：*]

已知幂级数为：
$ sum_(n=1)^(+oo) ((-1)^n 3^n)/n (x - 2)^n $

记 $a_n = ((-1)^n 3^n)/n$，根据比值法，求系数绝对值的比值极限：
$ rho = lim_(n -> +oo) |a_(n+1)/a_n| = lim_(n -> +oo) |((-1)^(n+1) 3^(n+1))/(n+1) times n/((-1)^n 3^n)| $
$ rho = lim_(n -> +oo) (3n)/(n+1) = 3 $

因此，收敛半径为：
$ R = 1/rho = 1/3 $

由 $|x - 2| < 1/3$ 可得收敛开区间：
$ -1/3 < x - 2 < 1/3 quad => quad 5/3 < x < 7/3 $

接下来讨论端点处的敛散性：

+ *当 $x = 7/3$ 时*，代入原级数得到：
  $ sum_(n=1)^(+oo) ((-1)^n 3^n)/n (1/3)^n = sum_(n=1)^(+oo) (-1)^n/n $
  这是一个交错级数。由于 $1/n$ 单调递减且 $lim_(n->+oo) 1/n = 0$，根据莱布尼茨判别法，该级数条件 *收敛*。

+ *当 $x = 5/3$ 时*，代入原级数得到：
  $ sum_(n=1)^(+oo) ((-1)^n 3^n)/n (-1/3)^n = sum_(n=1)^(+oo) ((-1)^n 3^n)/n ((-1)^n/3^n) = sum_(n=1)^(+oo) ((-1)^(2n))/n = sum_(n=1)^(+oo) 1/n $
  这是调和级数（即 $p=1$ 的 $p$ 级数），因此该级数 *发散*。

*综上所述：*
- 该幂级数的 *收敛半径* 为 $R = 1/3$
- 该幂级数的 *收敛域* 为 $(5/3, 7/3]$


 #text(fill: rgb("#eb3434"))[ *改编2*：]将函数 $f(x) = x arctan x - ln sqrt(1+x^2)$ 展开成关于 $x$ 的幂级数。


#text(fill: rgb("#eb3434"))[ *解答：*]




已知函数为：
$ f(x) = x arcsin x + sqrt(1-x^2) $

*第一步：展开 $x arcsin x$*
我们已知 $arcsin x$ 的幂级数展开式（当 $|x| < 1$ 时）为：
$ arcsin x = x + sum_(n=1)^(+oo) ((2n-1)!!)/((2n)!!(2n+1)) x^(2n+1) $
将其乘以 $x$，得到：
$ x arcsin x = x^2 + sum_(n=1)^(+oo) ((2n-1)!!)/((2n)!!(2n+1)) x^(2n+2) $

为了后续与 $sqrt(1-x^2)$ 的展开式合并同类项，我们作下标代换，令 $k = n + 1$（当 $n=1$ 时，$k=2$）：
$ x arcsin x = x^2 + sum_(k=2)^(+oo) ((2k-3)!!)/((2k-2)!!(2k-1)) x^(2k) $

*第二步：展开 $sqrt(1-x^2)$*
利用牛顿二项式定理展开 $(1 - x^2)^(1/2)$。当 $|x| < 1$ 时：
$ (1 - x^2)^(1/2) &= 1 + 1/2 (-x^2) + sum_(k=2)^(+oo) ((1/2)(1/2 - 1) dots.c (1/2 - k + 1))/(k!) (-x^2)^k \
&= 1 - 1/2 x^2 + sum_(k=2)^(+oo) ((-1)^(k-1) (2k-3)!!)/(2^k k!) (-1)^k x^(2k) $

由于 $2^k k! = (2k)!!$，且 $(-1)^(k-1) dot (-1)^k = -1$，上式可化简为：
$ sqrt(1-x^2) = 1 - 1/2 x^2 - sum_(k=2)^(+oo) ((2k-3)!!)/((2k)!!) x^(2k) $

*第三步：合并两项*
将上述两个展开式相加：
$ f(x) &= (x^2 + sum_(k=2)^(+oo) ((2k-3)!!)/((2k-2)!!(2k-1)) x^(2k)) + (1 - 1/2 x^2 - sum_(k=2)^(+oo) ((2k-3)!!)/((2k)!!) x^(2k)) \
&= 1 + 1/2 x^2 + sum_(k=2)^(+oo) [ ((2k-3)!!)/((2k-2)!!(2k-1)) - ((2k-3)!!)/((2k)!!) ] x^(2k) $

提取公因式以化简中括号内的系数（注意 $(2k)!! = 2k dot (2k-2)!!$）：
$ ((2k-3)!!)/((2k-2)!!) ( 1/(2k-1) - 1/(2k) ) &= ((2k-3)!!)/((2k-2)!!) dot 1/((2k-1)(2k)) \
&= ((2k-3)!!)/((2k)!! (2k-1)) $

代回原式，得到：
$ f(x) = 1 + 1/2 x^2 + sum_(k=2)^(+oo) ((2k-3)!!)/((2k)!!(2k-1)) x^(2k) $
（注：若将该式的下标换回 $n = k-1$，则与“先导后积”法求得的通项式 $((2n-1)!!)/((2n)!!(2n+1)(2n+2))$ 完全等价）。

*第四步：讨论收敛域*
两个级数 $x arcsin x$ 和 $sqrt(1-x^2)$ 的收敛半径均为 $R=1$，因此合并后的级数收敛半径也为 $R=1$。
当 $x = plus.minus 1$ 时，由斯特林公式可知一般项渐近于 $O(1/k^(5/2))$。根据 $p$ 级数判别法（因为 $p = 5/2 > 1$），级数在端点处绝对收敛。

*综上所述：*
- 幂级数展开式为：$f(x) = 1 + x^2/2 + sum_(k=2)^(+oo) ((2k-3)!!)/((2k)!!(2k-1)) x^(2k)$
- 该级数的 *收敛半径* 为 $R = 1$
- 该级数的 *收敛域* 为 $[-1, 1]$


#text(fill: rgb("#eb3434"))[ *改编题3：*]将函数 $f(x) = x cos x quad (0 <= x <= pi)$ 展开成正弦级数。

#text(fill: rgb("#eb3434"))[ *解答*：]

为了将定义在 $[0, pi]$ 上的函数 $f(x) = x cos x$ 展开为正弦级数，我们需要对其进行 *奇延拓*。延拓后的函数在 $[-pi, pi]$ 上为奇函数，此时傅里叶系数中 $a_n = 0$，只需计算正弦系数 $b_n$。

正弦级数的系数公式为：
$ b_n = 2/pi integral_0^pi f(x) sin(n x) dif x = 2/pi integral_0^pi x cos x sin(n x) dif x $

利用积化和差公式 $cos x sin(n x) = 1/2 [sin((n+1)x) + sin((n-1)x)]$，可得：
$ b_n = 1/pi integral_0^pi x [sin((n+1)x) + sin((n-1)x)] dif x $

对于一般项的积分 $integral_0^pi x sin(k x) dif x quad (k > 0)$，使用分部积分法：
$ integral_0^pi x sin(k x) dif x &= [ -x/k cos(k x) ]_0^pi - integral_0^pi (-1/k cos(k x)) dif x \
&= -pi/k cos(k pi) + [ 1/k^2 sin(k x) ]_0^pi \
&= -pi/k (-1)^k $

由于当 $n=1$ 时，$n-1=0$，上述分母为 $k$ 的公式不适用，因此我们需要分情况讨论：

*1. 当 $n = 1$ 时：*
$ b_1 &= 2/pi integral_0^pi x cos x sin x dif x \
&= 1/pi integral_0^pi x sin(2x) dif x $
直接代入上述分部积分结果（取 $k=2$）：
$ b_1 = 1/pi dot (-pi/2 (-1)^2) = -1/2 $

*2. 当 $n >= 2$ 时：*
分别代入 $k = n+1$ 和 $k = n-1$。
注意到 $cos((n+1)pi) = (-1)^(n+1) = -(-1)^n$，以及 $cos((n-1)pi) = (-1)^(n-1) = -(-1)^n$：
$ b_n &= 1/pi [ -pi/(n+1) (-1)^(n+1) - pi/(n-1) (-1)^(n-1) ] \
&= 1/pi [ pi/(n+1) (-1)^n + pi/(n-1) (-1)^n ] \
&= (-1)^n ( 1/(n+1) + 1/(n-1) ) \
&= (-1)^n (n-1 + n+1)/(n^2 - 1) \
&= (-1)^n (2n)/(n^2 - 1) $

*3. 讨论收敛域：*
根据狄利克雷收敛定理：
- 在开区间 $(0, pi)$ 上，原函数连续，级数收敛于 $f(x) = x cos x$。
- 当 $x = 0$ 时，级数各项为 $0$，和为 $0$，与 $f(0) = 0$ 一致。
- 当 $x = pi$ 时，级数各项为 $0$，和为 $0$。而原函数 $f(pi) = pi cos pi = -pi$。由于奇延拓后在 $x = plus.minus pi$ 处存在第一类间断点，级数收敛于左右极限的平均值，即 $(f(pi) + (-f(pi))) / 2 = 0$。

*综上所述：*
函数 $f(x) = x cos x$ 的正弦级数展开式为：
$ x cos x = -1/2 sin x + sum_(n=2)^(+oo) (-1)^n (2n)/(n^2 - 1) sin(n x) quad (0 <= x < pi) $


#text(fill: rgb("#eb3434"))[ *改编题4：*]证明：级数 $sum_(n=1)^(+oo) n^3/((3n+2)3^n)$ 收敛。

#text(fill: rgb("#eb3434"))[ *解答*：]

设该级数的一般项为 $a_n = n^3/((3n+2)3^n)$。由于对于所有 $n >= 1$，显然有 $a_n > 0$，因此这是一个正项级数。

我们将使用 *比值判别法* 来判断其敛散性。

首先，写出第 $n+1$ 项：
$ a_(n+1) = (n+1)^3/((3(n+1)+2)3^(n+1)) = (n+1)^3/((3n+5)3^(n+1)) $

计算前后两项的比值：
$ a_(n+1)/a_n &= (n+1)^3/((3n+5)3^(n+1)) dot ((3n+2)3^n)/n^3 \
&= ((n+1)/n)^3 dot (3n+2)/(3n+5) dot 3^n/3^(n+1) \
&= (1 + 1/n)^3 dot (3n+2)/(3n+5) dot 1/3 $

对该比值取当 $n -> +oo$ 时的极限：
$ rho &= lim_(n -> +oo) a_(n+1)/a_n \
&= lim_(n -> +oo) [ (1 + 1/n)^3 dot (3n+2)/(3n+5) dot 1/3 ] \
&= (lim_(n -> +oo) (1 + 1/n)^3) dot (lim_(n -> +oo) (3 + 2/n)/(3 + 5/n)) dot 1/3 \
&= 1^3 dot 1 dot 1/3 \
&= 1/3 $

*结论：*
因为极限值 $rho = 1/3 < 1$，根据正项级数的比值判别法可知，原级数 $sum_(n=1)^(+oo) n^3/((3n+2)3^n)$ *收敛*。

#text(fill: rgb("#eb3434"))[ *改编题5：*]判断级数 $sum_(n=2)^(+oo) (-1)^n / (n sqrt(ln n))$ 的敛散性，并说明是条件收敛还是绝对收敛。

#text(fill: rgb("#eb3434"))[ *解答*：]

原级数为交错级数：$sum_(n=2)^(+oo) (-1)^n u_n$，其中 $u_n = 1 / (n sqrt(ln n))$。

*第一步：判断是否绝对收敛*
考察其加绝对值后的级数：$sum_(n=2)^(+oo) |(-1)^n / (n sqrt(ln n))| = sum_(n=2)^(+oo) 1 / (n sqrt(ln n))$。

对于该正项级数，我们可以使用 *积分判别法*。
设对应的连续函数为 $f(x) = 1 / (x sqrt(ln x))$。
显然在区间 $[2, +oo)$ 上，$f(x)$ 连续、非负且单调递减。
计算其对应的广义积分：
$ integral_2^(+oo) 1 / (x sqrt(ln x)) dif x &= lim_(t -> +oo) integral_2^t (ln x)^(-1/2) dif (ln x) \
&= lim_(t -> +oo) [ 2 sqrt(ln x) ]_2^t \
&= lim_(t -> +oo) (2 sqrt(ln t) - 2 sqrt(ln 2)) \
&= +oo $
由于该广义积分发散，根据积分判别法，绝对值级数 $sum_(n=2)^(+oo) 1 / (n sqrt(ln n))$ *发散*。
因此，原级数 *不绝对收敛*。

*第二步：判断是否条件收敛*
回到原交错级数 $sum_(n=2)^(+oo) (-1)^n / (n sqrt(ln n))$。
我们使用 *莱布尼茨判别法* 来判断其敛散性，需要验证以下两个条件：

1. *单调递减*：当 $n >= 2$ 时，$n$ 单调递增，$ln n$ 也单调递增，且两者均为正数，故分母 $n sqrt(ln n)$ 单调递增。因此其倒数 $u_n = 1 / (n sqrt(ln n))$ 单调递减，即满足 $u_n >= u_(n+1)$。
2. *极限为零*：
   $ lim_(n -> +oo) u_n = lim_(n -> +oo) 1 / (n sqrt(ln n)) = 0 $

满足莱布尼茨判别法的两个条件，因此原交错级数 *收敛*。

*结论：*
由于原级数本身收敛，但其绝对值级数发散，因此该级数 *条件收敛*。


#text(fill: rgb("#eb3434"))[ *改编题6：*]将函数 $f(x) = x/(x^2 - 5x + 6)$ 展开成关于 $(x-1)$ 的幂级数，并写出成立的区间。

#text(fill: rgb("#eb3434"))[ *解答*：]

*第一步：因式分解与部分分式展开*
首先对原函数的分母进行因式分解：
$ f(x) = x/((x-2)(x-3)) $

利用待定系数法将其拆分为部分分式。设：
$ x/((x-2)(x-3)) = A/(x-2) + B/(x-3) $
通分并化简分子，得：$A(x-3) + B(x-2) = x$
- 令 $x = 2$，得 $-A = 2 quad => quad A = -2$
- 令 $x = 3$，得 $B = 3$

因此，原函数可化为：
$ f(x) = 3/(x-3) - 2/(x-2) $

*第二步：变量代换并构造几何级数形式*
题目要求展开成关于 $(x-1)$ 的幂级数。为了书写和计算方便，我们令 $t = x-1$，则 $x = t+1$。
将 $x$ 代入部分分式表达式中：
$ f(x) &= 3/((t+1)-3) - 2/((t+1)-2) \
&= 3/(t-2) - 2/(t-1) $

我们需要利用基础几何级数展开式 $1/(1-q) = sum_(n=0)^(+oo) q^n quad (|q| < 1)$，因此需将两项的分母分别凑成 $1-q$ 的形式：
- 第一项：$3/(t-2) = 3/(-2(1 - t/2)) = -3/2 dot 1/(1 - t/2)$
- 第二项：$-2/(t-1) = 2/(1-t)$

*第三步：逐项展开*
分别对两部分利用几何级数公式进行展开：
1. 对于第一项，要求公比 $|t/2| < 1$（即 $|t| < 2$）：
   $ -3/2 dot 1/(1 - t/2) = -3/2 sum_(n=0)^(+oo) (t/2)^n = sum_(n=0)^(+oo) (-3/2^(n+1)) t^n $
2. 对于第二项，要求公比 $|t| < 1$：
   $ 2/(1-t) = 2 sum_(n=0)^(+oo) t^n = sum_(n=0)^(+oo) 2 t^n $

*第四步：合并级数与确定收敛区间*
将两项级数相加，得到 $f(x)$ 关于 $t$ 的幂级数：
$ f(x) &= sum_(n=0)^(+oo) (-3/2^(n+1)) t^n + sum_(n=0)^(+oo) 2 t^n \
&= sum_(n=0)^(+oo) ( 2 - 3/2^(n+1) ) t^n $

将 $t = x-1$ 代回原变量，得到最终的幂级数：
$ f(x) = sum_(n=0)^(+oo) ( 2 - 3/2^(n+1) ) (x-1)^n $

接下来讨论收敛区间：
- 第一项的收敛条件为 $|x-1| < 2$，即 $-1 < x < 3$；
- 第二项的收敛条件为 $|x-1| < 1$，即 $0 < x < 2$。
合并级数的收敛域必须是两部分收敛域的 *交集*。因此，开区间为 $|x-1| < 1$，即 $(0, 2)$。

最后检验端点：
- 当 $x = 0$ 时，一般项为 $(2 - 3/2^(n+1)) (-1)^n$。当 $n -> +oo$ 时，该项不趋于 $0$，故发散。
- 当 $x = 2$ 时，一般项为 $(2 - 3/2^(n+1)) (1)^n$。当 $n -> +oo$ 时，极限趋于 $2$，不为 $0$，故发散。

*综上所述：*
- 该函数的幂级数展开式为：$f(x) = sum_(n=0)^(+oo) ( 2 - 3/2^(n+1) ) (x-1)^n$
- 展开式成立的区间为：$(0, 2)$


#text(fill: rgb("#eb3434"))[ *改编题7：*]判断级数 $sum_(n=1)^(+oo) (n^2 + 2n)/(4^n + (-3)^n)$ 的敛散性。

#text(fill: rgb("#eb3434"))[ *解答*：]

设级数的一般项为 $a_n = (n^2 + 2n)/(4^n + (-3)^n)$。

*第一步：判断级数的正负性*
观察 $a_n$ 的符号。对于所有的整数 $n >= 1$：
- 分子：$n^2 + 2n > 0$
- 分母：因为 $4^n > 3^n = |(-3)^n|$，所以 $4^n + (-3)^n >= 4^n - 3^n > 0$
因此，对于所有的 $n >= 1$ 均有 $a_n > 0$，故这是一个 *正项级数*。

*第二步：利用比值判别法求极限*
计算相邻两项的比值：
$ a_(n+1)/a_n &= ((n+1)^2 + 2(n+1))/(4^(n+1) + (-3)^(n+1)) dot (4^n + (-3)^n)/(n^2 + 2n) \
&= (n^2 + 4n + 3)/(n^2 + 2n) dot (4^n + (-3)^n)/(4^(n+1) + (-3)^(n+1)) $

为了在 $n -> +oo$ 时求极限，我们将分子和分母分别提取各自的主导项。前一个因式上下提取 $n^2$，后一个因式上下提取 $4^n$ 和 $4^(n+1)$：
$ a_(n+1)/a_n &= (n^2(1 + 4/n + 3/n^2))/(n^2(1 + 2/n)) dot (4^n(1 + (-3/4)^n))/(4^(n+1)(1 + (-3/4)^(n+1))) \
&= (1 + 4/n + 3/n^2)/(1 + 2/n) dot 1/4 dot (1 + (-3/4)^n)/(1 + (-3/4)^(n+1)) $

对该比值取极限：
已知当 $n -> +oo$ 时，$1/n -> 0$ 且指数函数 $(-3/4)^n -> 0$ （因为 $|-3/4| < 1$），可得：
$ rho &= lim_(n -> +oo) a_(n+1)/a_n \
&= (lim_(n -> +oo) (1 + 4/n + 3/n^2)/(1 + 2/n)) dot 1/4 dot (lim_(n -> +oo) (1 + (-3/4)^n)/(1 + (-3/4)^(n+1))) \
&= 1/1 dot 1/4 dot (1 + 0)/(1 + 0) \
&= 1/4 $

*结论：*
因为求得的极限值 $rho = 1/4 < 1$，根据正项级数的比值判别法，原级数 $sum_(n=1)^(+oo) (n^2 + 2n)/(4^n + (-3)^n)$ *收敛*。


#text(fill: rgb("#eb3434"))[ *改编题8：*]将函数 $f(x) = 1 - x/2 quad (0 <= x <= 2)$ 展开成周期为 4 的正弦级数，并求级数 $sum_(n=1)^(+oo) (-1)^(n-1) / (2n-1)$ 的和。

#text(fill: rgb("#eb3434"))[ *解答*：]

*第一步：求正弦级数的系数*
题目要求展开为周期 $T=4$ 的正弦级数，因此半周期 $L=2$。
为了得到正弦级数，我们需要对 $f(x)$ 在 $[-2, 0)$ 上进行奇延拓。
正弦级数的系数公式为：
$ b_n = 2/L integral_0^L f(x) sin((n pi x)/L) dif x = 2/2 integral_0^2 (1 - x/2) sin((n pi x)/2) dif x = integral_0^2 (1 - x/2) sin((n pi x)/2) dif x $

利用分部积分法计算上述积分：
令 $u = 1 - x/2$，$dif v = sin((n pi x)/2) dif x$。
则 $dif u = -1/2 dif x$，$v = -2/(n pi) cos((n pi x)/2)$。

$ b_n &= [ (1 - x/2) (-2/(n pi) cos((n pi x)/2)) ]_0^2 - integral_0^2 (-2/(n pi) cos((n pi x)/2)) (-1/2) dif x \
&= [ -2/(n pi) (1 - x/2) cos((n pi x)/2) ]_0^2 - 1/(n pi) integral_0^2 cos((n pi x)/2) dif x $

分别计算两部分：
1. 边界项代入：
   当 $x = 2$ 时，$1 - 2/2 = 0$，该项为 $0$。
   当 $x = 0$ 时，该项为 $-2/(n pi) dot (1 - 0) dot 1 = -2/(n pi)$。
   故边界项的值为 $0 - (-2/(n pi)) = 2/(n pi)$。
2. 积分项计算：
   $ 1/(n pi) integral_0^2 cos((n pi x)/2) dif x = 1/(n pi) [ 2/(n pi) sin((n pi x)/2) ]_0^2 = 2/(n^2 pi^2) (sin(n pi) - sin(0)) = 0 $

因此，傅里叶系数为 $b_n = 2/(n pi)$。

*第二步：写出正弦级数展开式*
根据狄利克雷收敛定理，在开区间 $(0, 2)$ 内，原函数满足连续可导的条件，级数收敛于原函数：
$ 1 - x/2 = sum_(n=1)^(+oo) 2/(n pi) sin((n pi x)/2) quad (0 < x < 2) $

*第三步：求常数项级数的和*
为了求出级数 $sum_(n=1)^(+oo) (-1)^(n-1) / (2n-1)$ 的和，我们需要在刚才求得的展开式中选择一个合适的 $x$ 值代入。
观察正弦项 $sin((n pi x)/2)$，为了产生交错符号且让偶数项消失，我们令 $x = 1$。
此时原函数的值为 $f(1) = 1 - 1/2 = 1/2$。

代入级数展开式得：
$ 1/2 = sum_(n=1)^(+oo) 2/(n pi) sin((n pi)/2) $

讨论 $sin((n pi)/2)$ 的取值：
- 当 $n = 2k$ （偶数）时，$sin(k pi) = 0$。
- 当 $n = 2k-1$ （奇数，$k=1, 2, 3, dots.c$）时，$sin(((2k-1) pi)/2) = (-1)^(k-1)$。

因此，级数中所有偶数项为 $0$，只有奇数项保留下来：
$ 1/2 = sum_(k=1)^(+oo) 2/((2k-1) pi) (-1)^(k-1) $

提取常数 $2/pi$：
$ 1/2 = 2/pi sum_(k=1)^(+oo) (-1)^(k-1)/(2k-1) $

两边同乘 $pi/2$，得到：
$ sum_(k=1)^(+oo) (-1)^(k-1)/(2k-1) = pi/4 $


*综上所述：*
- 函数的正弦级数展开式为：$f(x) = sum_(n=1)^(+oo) 2/(n pi) sin((n pi x)/2) quad (0 < x < 2)$
- 所求常数项级数的和为：$sum_(n=1)^(+oo) (-1)^(n-1) / (2n-1) = pi/4$


#text(fill: rgb("#eb3434"))[ *改编题9：*]求幂级数 $sum_(n=0)^(+oo) (1 - 1/(2n+2)) x^(2n+1)$ 在区间 $(-1, 1)$ 内的和函数 $S(x)$。

#text(fill: rgb("#eb3434"))[ *解答*：]

在区间 $(-1, 1)$ 内，幂级数绝对收敛，因此可以将一般项拆开分别求和：
$ S(x) = sum_(n=0)^(+oo) x^(2n+1) - sum_(n=0)^(+oo) 1/(2n+2) x^(2n+1) $

记 $S_1(x) = sum_(n=0)^(+oo) x^(2n+1)$，$S_2(x) = sum_(n=0)^(+oo) 1/(2n+2) x^(2n+1)$。

*第一步：计算 $S_1(x)$*
这是一个首项为 $x$，公比为 $x^2$ 的几何级数。当 $|x| < 1$ 时：
$ S_1(x) = x + x^3 + x^5 + dots.c = x/(1 - x^2) $

*第二步：计算 $S_2(x)$*
由于 $S_2(x) = 1/2 x + 1/4 x^3 + 1/6 x^5 + dots.c$，我们将级数乘以 $x$ 后再求导：
令 $f(x) = x S_2(x) = sum_(n=0)^(+oo) (x^(2n+2))/(2n+2)$
对 $f(x)$ 逐项求导（在收敛区间内）：
$ f'(x) = sum_(n=0)^(+oo) x^(2n+1) = x + x^3 + x^5 + dots.c = x/(1 - x^2) $

接下来对 $f'(x)$ 在 $[0, x]$ 上积分求回 $f(x)$：
$ f(x) &= integral_0^x t/(1 - t^2) dif t \
&= - 1/2 integral_0^x 1/(1 - t^2) dif (1 - t^2) \
&= - 1/2 [ ln(1 - t^2) ]_0^x \
&= - 1/2 ln(1 - x^2) $

因此：
$ S_2(x) = f(x)/x = - (ln(1 - x^2))/(2x) quad (x != 0) $
当 $x = 0$ 时，显然 $S_2(0) = 0$。由极限可知 $lim_(x->0) S_2(x) = 0$，故和函数在收敛区间内是连续的。

*第三步：求最终和函数 $S(x)$*
将两部分结果合并：
$ S(x) = S_1(x) - S_2(x) = x/(1 - x^2) + (ln(1 - x^2))/(2x) $

需要注意：当 $x = 0$ 时，根据原级数定义，$S(0) = 0$。而上述表达式在 $x=0$ 处的形式为 $0/0$ 型极限，通过泰勒展开可知该极限值确实为 $0$。

*综上所述：*
该幂级数的和函数为：
$ S(x) = cases(
  x/(1 - x^2) + (ln(1 - x^2))/(2x) & quad x != 0,
  0 & quad x = 0
) $
其中成立的区间为 $(-1, 1)$。