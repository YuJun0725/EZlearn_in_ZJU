#set page(
  paper: "a4",
  margin: (x: 2cm, y: 2.5cm)
)
#set text(
  font: ("New Computer Modern", "Source Han Serif SC", "SimSun"),
  size: 11pt
)
#set heading(numbering: "1.1")
#show heading: it => [
  #v(0.5em)
  #it
  #v(0.5em)
]
#let dif = $upright(d)$


#show heading: set text(fill: gradient.linear(red, blue))
= 无穷级数知识点

== 数项级数

- *部分和 ：* 将无穷级数 $sum_(n=1)^oo u_n$ 的前 $n$ 项相加，记为 $S_n = sum_(k=1)^n u_k$。
- *敛散性判断：* 级数的敛散性完全取决于部分和数列 ${S_n}$ 的极限。
  - 若 $lim_(n -> oo) S_n = S$ ($S$ 为常数)，则级数*收敛*，和为 $S$。
  - 若极限不存在或为无穷大，则级数*发散*。

  #text(fill: rgb("#3490eb"))[  注：这种一般适用于部分和能求出来的级数，类似高中数学的数列求和。]

---

- 两个最常见的级数

在后续的比较判别法中，这两个级数会经常用来辅助我们判断复杂级数的敛散性：

*几何级数 (类似等比数列)：* $sum_(n=0)^oo a q^n quad (a != 0)$
  - $|q| < 1$ 时，*收敛*，和为 $a/(1-q)$。
  - $|q| >= 1$ 时，*发散*。

*p-级数：* $sum_(n=1)^oo 1/n^p$
  - $p > 1$ 时，*收敛*。（感性的理解就是分母很大所以收敛）
  - $p <= 1$ 时，*发散*。（特别地，$p=1$ 时为调和级数 $sum 1/n$，发散）。

== 数项级数基本性质

- *线性运算：* 若 $sum u_n = A$，$sum v_n = B$，则 $sum (alpha u_n + beta v_n) = alpha A + beta B$。
- *有限项无用：* 在级数中增加、删除或修改*有限项*，不会改变级数最终的敛散性（但会改变收敛时的和，毕竟有限项还是有实打实的数据的）。
- *结合性：* 对*收敛*的级数，任意加括号后形成的新级数依然收敛，且和不变。（注意：对发散级数随意加括号可能会变成收敛，例如 $1-1+1-1 dots$ 加上括号变成 $(1-1)+(1-1) dots=0$ 是错误的）。
- * 收敛的必要条件：*
  - 如果级数 $sum u_n$ 收敛，那么必然有 $lim_(n -> oo) u_n = 0$。
  - *逆否命题（极其重要）：* 如果通项的极限 $lim_(n -> oo) u_n != 0$ 或者不存在，那么级数*必定发散*。这是做所有级数题的“第一把斧子”。
  

   #text(fill: rgb("#3490eb"))[  注：一般判断敛散性的时候第一步看这两个。]


== 正项级数敛散性判别法
定义：（顾名思义）正项级数满足 $u_n >= 0$。其部分和 $S_n$ 是单调递增的，性质很美丽，因此判别方法非常丰富。

---


- *收敛充要条件：* 部分和数列 ${S_n}$ 有上界。
- *比较判别法：* 找一个已知敛散性的级数 $v_n$。
  - 若 $u_n <= v_n$，且大级数 $sum v_n$ 收敛 $=>$ 小级数 $sum u_n$ 收敛。
  - 若 $u_n >= v_n$，且小级数 $sum v_n$ 发散 $=>$ 大级数 $sum u_n$ 发散。
- *极限比较判别法：* 设 $lim_(n -> oo) u_n/v_n = l$。若 $0 < l < +oo$，两者同敛散。（感性理解为两个数列成比例，故同敛散）
- *比值判别法：* 设 $lim_(n -> oo) u_(n+1)/u_n = rho$。（注意这个是同一个级数的前后项比值，而不是前面那种2个不同级数的比值）
  - $rho < 1$：收敛； $rho > 1$：发散； $rho = 1$：*失效*。（感性理解为后一项比前一项小，整个数列越来越小了，反之同理）
- *根值判别法：* 设 $lim_(n -> oo) root(n, u_n) = rho$。结论同比值判别法，且根值法比比值法更强。
- *积分判别法：* 将 $u_n$ 转化为连续函数 $f(x)$，若 $f(x)$ 在 $[1, +oo)$ 上非负、单调递减，则 $sum u_n$ 与广义积分 $integral_1^(+oo) f(x) dif x$ 同敛散。（个人感觉不咋用，一般用用的时候都是式子里会有lnx、1/x这样的结构，因为$1/x "dx"="d(lnx)"$）



== 一般数项级数 (有正有负)

=== 交错级数与莱布尼茨定理
形式为 $sum_(n=1)^oo (-1)^(n-1) u_n$ （其中 $u_n > 0$）。
- *适用条件 (莱布尼茨定理)：*
  + ${u_n}$ 单调递减（即 $u_(n+1) <= u_n$）。（其实单调递增也行，只要提出一个负数就行）
  + $lim_(n -> oo) u_n = 0$。
  同时满足这两点，交错级数必定收敛。
- *推论（误差估计）：* 用前 $n$ 项和 $S_n$ 近似总和 $S$ 时，截断误差不超过第一个被舍去的项的绝对值，即 $|S - S_n| <= u_(n+1)$。

=== 绝对收敛与条件收敛
- *绝对收敛：* 若加上绝对值后的正项级数 $sum |u_n|$ 收敛，则原级数 $sum u_n$ 必定收敛，且称为*绝对收敛*。
- *条件收敛：* 若原级数 $sum u_n$ 收敛，但 $sum |u_n|$ 发散，则称原级数为*条件收敛*。

=== 绝对值比值/根值判别法 
- *正常的判别法：* 只能用于*正项级数*（所有项都是正的）。
- *绝对值判别法：* 注意区别于前面的比值判别法，因为前面那个是只适用于正项级数，而这个绝对值是用于项的符号不规律的级数。方法是*先给通项加上绝对值*变成正项级数，然后再求极限 $rho = lim_(n -> oo) abs(u_(n+1)/u_n)$ 或 $rho = lim_(n -> oo) root(n, |u_n|)$。
  - 若 $rho < 1$，则原级数*绝对收敛*。
  - 若 $rho > 1$，则原级数*发散*（注意，不是条件收敛，是直接发散）。

== 幂级数

=== 核心定理与公式
- *阿贝尔定理：* 幂级数 $sum a_n x^n$ 若在 $x = x_0 quad (x_0 != 0)$ 处收敛，则对于所有满足 $|x| < |x_0|$ 的 $x$，级数不仅收敛，而且绝对收敛；若在 $x = x_1$ 处发散，则对所有 $|x| > |x_1|$ 的 $x$ 皆发散。
- *柯西-阿达玛公式 (求收敛半径 $R$)：*
  - 比值形式：$R = lim_(n -> oo) abs(a_n/a_(n+1))$
  - 根值形式：$R = 1 / lim_(n -> oo) root(n, |a_n|)$

=== 概念辨析：收敛半径 vs 收敛区间 vs 收敛域
- *收敛半径 $R$：* 决定了以原点为中心的“安全圆”，在距离原点 $R$ 以内绝对收敛。
- *收敛区间：* 开区间 $(-R, R)$。
- *收敛域：* 需要*单独把端点 $x = R$ 和 $x = -R$ 代入原级数*，根据实际情况判断边界是不是处在收敛域中（利用数项级数的方法判断敛散性）。收敛域可能是 $(-R, R)$、$[-R, R)$、$(-R, R]$ 或 $[-R, R]$ 中的任意一种。

=== 和函数及其三个性质
在收敛域内，级数的和是一个关于 $x$ 的函数，即和函数 $S(x)$。它在 $(-R, R)$ 内有很多性质：
- *连续性：* $S(x)$ 在收敛域内连续，因为 $(-R, R)$ 包含于收敛域，所以$(-R, R)$ 内一定连续。（相信你能很好的区分$(-R, R)$ 和收敛域的关系）
- *逐项可导：* $S'(x) = sum_(n=1)^oo n a_n x^(n-1)$，且求导后的新幂级数收敛半径 $R$ 不变。
- *逐项可积：* $integral_1^x S(t) dif t = sum_(n=0)^oo a_n/(n+1) x^(n+1)$，积分后的新幂级数收敛半径 $R$ 也不变。
- *无限可导性与唯一性：* $S(x)$ 在收敛区间内可以无限次求导，且一个函数如果能展开成幂级数，其展开式是*唯一的*。

== 函数的级数展开

=== 泰勒展开 & 麦克劳林展开
将复杂的非线性函数局部近似为无穷多项式。
- *泰勒展开：* 在 $x=x_0$ 处的展开式：
  $ f(x) = sum_(n=0)^oo (f^((n))(x_0))/(n!) (x - x_0)^n $
- *麦克劳林展开：* 特指在 $x_0 = 0$ 处的泰勒展开。


  // (1) 指数函数
$ e^x = 1 + x + x^2 / 2! + dots + x^n / n! + dots, quad x in (-oo, +oo) $

// (2) 正弦函数
$ sin x = x - x^3 / 3! + x^5 / 5! - dots + (-1)^n x^(2n+1) / ((2n+1)!) + dots, quad x in (-oo, +oo) $

// (3) 余弦函数
$ cos x = 1 - x^2 / 2! + x^4 / 4! - x^6 / 6! + dots + (-1)^n x^(2n) / ((2n)!) + dots, quad x in (-oo, +oo) $

// (4) 对数函数
$ ln(1+x) = x - x^2 / 2 + x^3 / 3 - x^4 / 4 + dots + (-1)^n x^(n+1) / (n+1) + dots, quad x in (-1, 1] $

// (5) 二项展开式
$ (1+x)^alpha = 1 + alpha x + (alpha(alpha-1)) / 2! x^2 + dots + (alpha(alpha-1)dots(alpha-n+1)) / n! x^n + dots, quad x in (-1, 1) $



== 傅里叶级数

=== 核心思想
任何周期函数（即使包含尖角或断点）都可以看作是由无限多个不同频率、不同振幅的正弦波和余弦波叠加而成。


=== 三角函数系的正交性
傅里叶展开的基础在于三角函数系在区间 $[-pi, pi]$ 上的正交性：
$ integral_(-pi)^pi cos(n x) cos(m x) dif x = 0 quad (n != m) $
$ integral_(-pi)^pi sin(n x) sin(m x) dif x = 0 quad (n != m) $
$ integral_(-pi)^pi sin(n x) cos(m x) dif x = 0 quad ("对任意" n, m) $


若函数周期为 $T = 2L$，则其展开式为：
$ f(x) = a_0 / 2 + sum_(n=1)^oo (a_n cos((n pi x) / L) + b_n sin((n pi x) / L)) $


 #text(fill: rgb("#3490eb"))[  注意第一项有1/2]


区别于前面的比值判别法，因为前面那个是只适用于正项级数，而这个绝对值是用于项的符号不规律的级数。方法是
其中系数为：
$ a_0 = 1/L integral_(-L)^L f(x) dif x $
$ a_n = 1/L integral_(-L)^L f(x) cos((n pi x) / L) dif x $
$ b_n = 1/L integral_(-L)^L f(x) sin((n pi x) / L) dif x $

=== 狄利克雷收敛定理
设 $f(x)$ 是周期为 $2L$ 的周期函数，且在 $[-L, L]$ 上满足：
1. 连续或只有有限个第一类间断点；
2. 只有有限个极值点。
则其傅里叶级数在 $x$ 处收敛于 $S(x)$：
- 当 $x$ 是连续点时：$S(x) = f(x)$
- 当 $x$ 是间断点时：$S(x) = (f(x^-) + f(x^+)) / 2$





== 历年题改编和总结

说在前面：由于答案在路老师的公众号里还是比较详细，这里就不赘述了。本部分主要对题目思路进行一定梳理总结，以及会有改编题在后面附上，感兴趣的可以做一做。

---


 #text(fill: rgb("#eb3434"))[ *原题1*：]
求幂级数 $sum_(n=1)^(+oo) (-1)^n 2^n/sqrt(n) (x - 1/2)^n$ 的收敛半径与收敛域。

 #text(fill: rgb("#eb3434"))[ *解答：*]

1. 看到求解收敛半径和收敛域，则第一步用比值/根值方法求得收敛半径（这个方法根据系数形式来选择，显然，系数有很多指数，适合用比值法）
2. 然后根据求得的半径得到收敛区间
3. #text(fill: rgb("#34aeeb"))[ 将边界代入]，判断级数是否收敛，最后得到收敛域。


#text(fill: rgb("#eb3434"))[ *改编题：*]求幂级数 $sum_(n=1)^(+oo) (-1)^n 3^n/n (x - 2)^n$ 的收敛半径与收敛域。

---

 #text(fill: rgb("#eb3434"))[ *原题2*：]将函数 $f(x) = x arctan x - ln sqrt(1+x^2)$ 展开成关于 $x$ 的幂级数。

 #text(fill: rgb("#eb3434"))[ *解答：*]

这种题首先要熟悉常见式子的幂级数展开推导方法和思路，很多题目都是这些常见展开的组合。

一切常见展开（如对数函数、反三角函数）的基础，#text(fill: rgb("#34aeeb"))[ 均源自*无穷等比级数求和公式*。]
当 $|q| < 1$ 时：
$ 1/(1-q) = sum_(n=0)^oo q^n = 1 + q + q^2 + q^3 + dots.c $

*$arctan x$* 

+ 求导之后得到$ 1/(1+t^2)$
  在母公式中令 $q = -t^2$，当 $|t| < 1$ 时，得：
  $ 1/(1+t^2) = sum_(n=0)^oo (-t^2)^n = sum_(n=0)^oo (-1)^n t^(2n) $

+ 
  对等式两边在 $[0, x]$ 上积分（要求 $|x| < 1$）：
  $ integral_0^x 1/(1+t^2) dif t = integral_0^x (sum_(n=0)^oo (-1)^n t^(2n)) dif t $

+ 
  左端原函数为 $arctan t$，右端逐项积分：
  $ arctan x = sum_(n=0)^oo (-1)^n x^(2n+1)/(2n+1) quad (x in [-1, 1]) $


* $ln(1+x)$ *

+ 求导之后得到 $ 1/(1+t)$
  在母公式中令 $q = -t$，当 $|t| < 1$ 时，得：
  $ 1/(1+t) = sum_(n=0)^oo (-t)^n = sum_(n=0)^oo (-1)^n t^n $

+ 
  对等式两边在 $[0, x]$ 上积分：
  $ integral_0^x 1/(1+t) dif t = integral_0^x (sum_(n=0)^oo (-1)^n t^n) dif t $

+ 
  左端原函数为 $ln(1+t)$，右端逐项积分：
  $ ln(1+x) = sum_(n=0)^oo (-1)^n x^(n+1)/(n+1) quad (x in (-1, 1]) $

其他式子可见课本P286例2，此处不多赘述啦

总结一下，拿到式子一般会有3种处理：
1. 直接搞
2. 求导后搞，然后再积分回来
3. 积分后搞，然后再求导回来

#text(fill: rgb("#34aeeb"))[一般求导/积分之后就可以看到很多常见式子的组合 ]，这时候就可以根据我们之前已经了解过的常见的解决方法处理

 #text(fill: rgb("#eb3434"))[ *改编题*：]将函数 $f(x) = x arcsin x + sqrt(1-x^2)$ 展开成关于 $x$ 的幂级数。

---


 #text(fill: rgb("#eb3434"))[ *原题3*：]将函数 $f(x) = cos x quad (0 <= x <= pi)$ 展开成正弦级数。

 #text(fill: rgb("#eb3434"))[ *解答：*]
 
 看题目说的是正弦级数，只有当F(x)为奇函数的时候会完全保留正弦项，故对其进行奇延拓

以防你不知道什么是#text(fill: rgb("#34aeeb"))[奇延拓和偶延拓 ]：

设函数 $f(x)$ 定义在区间 $[0, L]$ 上。为了在 $[-L, L]$ 上进行傅里叶展开，我们定义延拓函数 $F(x)$。

奇延拓

*定义*：将 $f(x)$ 延拓为定义在 $[-L, L]$ 上的奇函数：
$ F(x) = cases(
  f(x) quad & 0 < x < L,
  0 quad & x = 0", " plus.minus L,
  -f(-x) quad & -L < x < 0
) $

*特点*：
- 延拓后的函数 $F(x)$ 是奇函数，关于**原点**对称。
- 展开后所有的余弦项系数 $a_n = 0$。
- 得到的是*正弦级数*：$f(x) = sum_(n=1)^oo b_n sin((n pi x) / L)$。
- 其中 $b_n = 2/L integral_0^L f(x) sin((n pi x) / L) dif x$。

偶延拓

*定义*：将 $f(x)$ 延拓为定义在 $[-L, L]$ 上的偶函数：
$ F(x) = cases(
  f(x) quad & 0 <= x <= L,
  f(-x) quad & -L <= x < 0
) $

*特点*：
- 延拓后的函数 $F(x)$ 是偶函数，关于 **$y$ 轴**对称。
- 展开后所有的正弦项系数 $b_n = 0$。
- 得到的是*余弦级数*：$f(x) = a_0/2 + sum_(n=1)^oo a_n cos((n pi x) / L)$。
- 其中 $a_n = 2/L integral_0^L f(x) cos((n pi x) / L) dif x$。


以防你不知道这样一个傅里叶级数的性质：

- *如果 $F(x)$ 是奇函数*（即 $F(-x) = -F(x)$）：由于余弦函数 $cos(n x)$ 是偶函数，那么奇函数乘偶函数等于奇函数。奇函数在对称区间 $[-pi, pi]$ 上的积分为 $0$。因此，所有的 $a_n$（包括常数项 $a_0$）全部为 $0$，展开式中只剩下正弦项（$b_n$），即*正弦级数*。

- *如果 $F(x)$ 是偶函数*（即 $F(-x) = F(x)$）：同理，所有的正弦项系数 $b_n$ 全部为 $0$，展开式中只剩下常数项和余弦项，这就是*余弦级数*。

知道这些之后就可以直接用求展开式的公式了，具体可见课本或上面总结。

这种让你求傅里叶展开级数的题目也是比较固定的，都是直接带公式（当然首先要明确题目要求，判断是否需要奇延拓和偶延拓）

 #text(fill: rgb("#eb3434"))[ *改编题*：]将函数 $f(x) = x cos x quad (0 <= x <= pi)$ 展开成正弦级数。

---



 #text(fill: rgb("#eb3434"))[ *原题4*：]证明：级数 $sum_(n=1)^(+oo) n^2/((2n+1)2^n)$ 收敛。

 #text(fill: rgb("#eb3434"))[ *解答：*]
 
 这个是非常常见的证明收敛/发散题，不妨思考一下有哪些证明收敛/发散的方法。

我认为主要有：
等等先别急，先判断数列种类，一般是三种：正项级数；一般级数；交错级数，根据不同的种类选择不同的判定方法

一般有：
0. 定义法
就是证明那个部分和收敛，正如前文说到，这个适合能求出部分和公式的数列，或者经过放缩的数列能够求出部分和。

1. 自己和自己比
对于正项级数，可以前后项做比值或用根值判别法

对于非交错级数，可以用绝对值进行比值和根值判别

对于交错级数，那就大概率可以尝试用莱布尼兹法（注意单调的使用条件）

根值判别法：
2. 自己和别人比（就相当于是再找一个级数来进行比较）：
对于正项级数，可以和另一个级数比大小、可以做比值，这个另一个级数一般可以通过放缩得到，或者用p-级数也是可以的，因为p-级数的收敛性我们是很清楚的。


反观这道题，主要有指数和多项式存在，可以考虑用比值法，这样消去了指数，留下多项式，计算极限值很容易。

 #text(fill: rgb("#eb3434"))[ *改编题*：]证明：级数 $sum_(n=1)^(+oo) n^3/((3n+2)3^n)$ 收敛。

---


 #text(fill: rgb("#eb3434"))[ *原题5*：]判断级数 $sum_(n=1)^(+oo) (-1)^(n-1)/(n ln(n+1))$ 的敛散性，并据理说明是条件收敛还是绝对收敛。

 #text(fill: rgb("#eb3434"))[ *解答：*]


首先，考察其绝对值级数 $sum_(n=1)^(+oo) 1/(n ln(n+1))$ 的敛散性。这种形式比较棘手，不论是使用比值还是根值最后形式都不会好看，比较难求极限。故我们考虑找别的级数来辅助比较。可以用 $sum_(n=1)^(+oo) 1/(n ln n)$ 首先形式最接近，其次，这个式子我们是知道他是发散的（书本里积分判别法的例子就是，可以去详细看一下或者简单记忆一下，一般积分判别法用的也不多，考题基本就是这种$1/x "dx"="d(lnx)"$形式的。


接着，发现级数符合交错级数的条件，故使用莱布尼兹判别法会比较简单

 #text(fill: rgb("#eb3434"))[ *改编题*：]判断级数 $sum_(n=2)^(+oo) (-1)^n / (n sqrt(ln n))$ 的敛散性，并说明是条件收敛还是绝对收敛。

 ---




 #text(fill: rgb("#eb3434"))[ *原题6*：]将函数 $f(x) = 1/(x^2 - 2x - 3)$ 展开成关于 $(x-1)$ 的幂级数，并写出成立的区间。

 #text(fill: rgb("#eb3434"))[ *解答：*]


分母可以因式分解，然后进行换元$x = t + 1$ ，并将式子凑成基本展开式 $1/(1-q)$ 的形式，接下来利用无穷等比级数求和公式 $1/(1-q) = sum_(n=0)^oo q^n$即可。

*最后不要忘记求求成立的区间*：
展开的前提条件是 $|t| < 2$，即 $|x - 1| < 2$，解得 $-1 < x < 3$。
检查端点：当 $x = 3$ 或 $x = -1$ 时，级数的一般项为 $-1/4$，极限不为零，级数发散。
所以展开式成立的区间为 $(-1, 3)$。


很多人前面其实是会做的，#text(fill: rgb("#34aeeb"))[但总是忘记求后面成立的区间（比如我]

 #text(fill: rgb("#eb3434"))[ *改编题*：]将函数 $f(x) = x/(x^2 - 5x + 6)$ 展开成关于 $(x-1)$ 的幂级数，并写出成立的区间。

---


 #text(fill: rgb("#eb3434"))[ *原题7*：]判断级数 $sum_(n=1)^(+oo) (n+1)/(3^n + (-2)^n)$ 的敛散性。

*解答*：
先看形式，发现有指数，但是很烦人，分母是2个不同底数的指数相加的形式（恼），用比值无法消去，这下应该怎么办啊。。。

不慌，可以看到其中一个指数是负数，另一个则是$3^n$，这意味着在n很大的时候分母会很大，然后$(-2)^n$对分母的影响实际是微乎其微的。这样我们能够有这样感谢的认识，就是当n非常大的时候，整个通项实际上是和$ (n+1)/(3^n$非常接近的。那不就可以把他当做另一个用来辅助判断敛散性的工具了吗（好耶。接下来相信你会做了，就是用两个不同数列的比值审敛法。

这道题给我们的启示是，其实很多时候我们可以观察式子的形状，看他在n非常大的时候能不能感性等效于其他式子，#text(fill: rgb("#34aeeb"))[或者式子中哪些部分是起到决定性作用的]。常见的等效比如说取分子Or分母幂次最大的多项式（因为他起到决定性因素），以及指数相比多项式影响更大。

 #text(fill: rgb("#eb3434"))[ *改编题*：]判断级数 $sum_(n=1)^(+oo) (n^2 + 2n)/(4^n + (-3)^n)$ 的敛散性。

---

 #text(fill: rgb("#eb3434"))[ *原题8*：]叙述常数项级数收敛的 Cauchy 收敛准则，并利用 Cauchy 收敛准则证明调和级数 $sum_(n=1)^(+oo) 1/n$ 发散。

 #text(fill: rgb("#eb3434"))[ *解答：*]
 
 *以防你不知道Cauchy收敛准则：*
常数项级数 $sum_(n=1)^(+oo) a_n$ 收敛的充分必要条件是：对于任意给定的正数 $epsilon > 0$，总存在正整数 $N$，使得当 $n > N$ 时，对于任意正整数 $p$，都有：
$ |a_(n+1) + a_(n+2) + dots.c + a_(n+p)| < epsilon $

接下来其实是课本的解法，这部分可以看看书。

---


 #text(fill: rgb("#eb3434"))[ *原题9*：]将函数 $f(x) = x - 1 quad (0 <= x <= 2)$ 展开成周期为 4 的余弦级数，并求级数 $sum_(n=1)^(+oo) 1/(2n-1)^2$ 的和。

第一问比较常规，和前面原题3是一样的，不多赘述。

求得$ x - 1 = -8/pi^2 sum_(k=1)^oo 1/(2k-1)^2 cos(((2k-1) pi x)/2) quad (0 <= x <= 2) $

然后题目要我们求的是一个具体级数的值，这个的思路就是利用我们刚刚求出来的式子，然后代入一些值，就可以得到代求式子的值了。这里的话就是将0代入（通过观察式子形式不难看出，因为要求的式子里没有三角函数项，一般让每一个三角函数都为1/0）

 #text(fill: rgb("#eb3434"))[ *改编题*：]将函数 $f(x) = 1 - x/2 quad (0 <= x <= 2)$ 展开成周期为 4 的正弦级数，并求级数 $sum_(n=1)^(+oo) (-1)^(n-1) / (2n-1)$ 的和。


---


 #text(fill: rgb("#eb3434"))[ *原题10*：]求幂级数 $sum_(n=1)^(+oo) (1 - 1/(2n+1)) x^(2n)$ 在区间 $(-1, 1)$ 内的和函数 $S(x)$。

  #text(fill: rgb("#eb3434"))[ *解答：*]

对于这种x前面系数比较复杂的式子，可以试着分解，分出几个可以求解的并且简单的。比如：
$ S(x) = sum_(n=1)^(+oo) x^(2n) - sum_(n=1)^(+oo) 1/(2n+1) x^(2n) $




第一个式子很简单，就是基本的等比数列求和，$ S_1(x) = (x^2) / (1 - x^2) $第二个也不难，可以看到系数是$1/(2n+1)$，一般这种含有n的一次项式子放在分母的，我们经常用求导解决（因为幂函数求导法则的特性），但是明显现在的幂次是2n，怎么办呢，好办啊，再乘上一个x就行$ x S_2(x) = sum_(n=1)^(+oo) 1/(2n+1) x^(2n+1) $

接下来相信你已经知道怎么做了，就是求导，求和，再积分回来。然后将两部分合并得到最终结果

注：积分的时候下限一般都取0，因为F(0)最好算；然后$x S_2(x) = sum_(n=1)^(+oo) 1/(2n+1) x^(2n+1) $这个式子求出$S_2$的时候是要除以x的，#text(fill: rgb("#34aeeb"))[这就要对x=0进行讨论！！！！]注意这个非常重要。最后得到的和函数是分段函数！！！

 #text(fill: rgb("#eb3434"))[ *改编题*：]求幂级数 $sum_(n=0)^(+oo) (1 - 1/(2n+2)) x^(2n+1)$ 在区间 $(-1, 1)$ 内的和函数 $S(x)$。



== 说在最后

期中考加油哦\~